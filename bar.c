typedef unsigned long long ull;
;

struct Node {
    int id;
    char *name;
};

const int step = 1;

typedef float (*F)(long **);

F helper(double *arg) {
    return 0;
}

int main() {
    99.0 + 'c';
    int *(*func0())(char *);
    float (*(*const func1)(double *))(long **) = helper;
    char *const cccccc;
    const int tttttt = 0xfffffffffffffff;
    struct Node n = {0, "Michael"};
    int test = (unsigned char)(int)0xffffffff;
    (unsigned char)(step ? n.id + 1 : 0);
    int sum = 0;
    sum += 10;
    for (int i = 1; i <= 9; ++i) {
        sum += i;
    }
    unsigned long int test_invalid_type;
    test = (struct Node){test, 0}.id;
    ull sz0 = sizeof(struct Node){test, 0};
    ull sz1 = sizeof ++test;
    n.id = sum + step * sum++ % 10;
    int a, b, c = 1;
    a = b -= c *= 10;
    const int *const specQual;
    1 * 2 + 3 - 4 % 5 ? 99 : 999;
    3.14159 * (2 + 3.f * 0xbeef) - 4l / 5.5e1 ? 99 + 0333 - 0123.123e4 : 0x11p+3 + 0xbeefp1;
    return 0;
}